Delete the blank space
  #include <stdio.h>

int main() {
    char str[200];
    char ch;
    int i = 0;

    while ((ch = getchar()) != '\n' && ch != EOF)
        if (ch != ' ')
            str[i++] = ch;

    str[i] = '\0';
    printf("%s\n", str);
    return 0;
}
