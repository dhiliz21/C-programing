EVEN OR ODD

#include <stdio.h>
int main() {
    int num;
    scanf("%d", &num);
    if (num % 2 == 0) {
        printf("The given input %d is Even\n", num);
    } else {
        printf("The given input %d is Odd\n", num);
    }
    return 0;
}
