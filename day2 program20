Product of Range

#include <stdio.h>
int main() {
    int n, product = 1;
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) {
        product *= i;
    }
    printf("Product of value is %d\n", product);
    return 0;
}
